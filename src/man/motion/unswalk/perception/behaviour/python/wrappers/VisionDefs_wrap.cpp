scope().attr("TOP_IMAGE_ROWS") = TOP_IMAGE_ROWS;
scope().attr("TOP_IMAGE_COLS") = TOP_IMAGE_COLS;
scope().attr("BOT_IMAGE_ROWS") = BOT_IMAGE_ROWS;
scope().attr("BOT_IMAGE_COLS") = BOT_IMAGE_COLS;
scope().attr("IMAGE_ROWS") = IMAGE_ROWS;
scope().attr("IMAGE_COLS") = IMAGE_COLS;
scope().attr("IMAGE_HFOV") = IMAGE_HFOV;
