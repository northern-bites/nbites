Edit 10/4/11 (Lizzie) Added a toolbar with prev/next buttons to this module so that it doesn't
have to rely on ColorCreator's buttons. Also fixed a bug that caused the image to often not
show up or update in the viewer. 
