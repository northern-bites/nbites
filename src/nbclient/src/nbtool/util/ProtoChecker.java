package nbtool.util;

/*
import java.io.File;
import java.io.IOException;
import java.nio.file.FileSystems;
import java.nio.file.Files;
import java.nio.file.Path;
import java.util.Iterator;
import java.util.stream.Stream;

public class ProtoChecker {

	
	public static void main(String[] args) {
		if (args.length == 0) {
			U.w("usage: java nbtool.util.ProtoChecker filenames > exception_file");
			return;
		}
		
		U.w("------------GENERATED BY PROTOCHECKER :: DO NOT MODIFY--------------");
		for (String fname : args) {
			//boolean FOUND_CORE = false;
			Path path = FileSystems.getDefault().getPath(fname);
			String fname_actual = path.getFileName().toString();
			
			Stream<String> stream = null;
			try {
				stream = Files.lines(path);
			} catch (IOException e) {
				// TODO Auto-generated catch block
				e.printStackTrace();
			}
			Iterator<String> itr = stream.iterator();
			
			while(itr.hasNext()) {
				String ln = itr.next();
				
				if (ln.startsWith("message")) {
					String[] words = ln.split(" ");
					String[] OCn = fname_actual.split("\\.");
					
					if (!words[1].equalsIgnoreCase(OCn[0])) {
						//U.w(words[1] + " != " + OCn[0] + " in file " + fname_actual);
						U.w(words[1] + "= " + OCn[0]);
					}
				}
			}
		}
	}
}

*/
